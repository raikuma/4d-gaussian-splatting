목표:
동일 품질을 유지하면서 더 빠르게 학습을 수행하는 4DGS 모델 연구하기

아이디어:
- 학습 파이프라인을 이미지 단위에서 16x16 크기의 tile 단위로 변경하고, 학습이 필요한 tile만 선별적으로 학습하여 학습 속도를 향상.
- 학습이 필요한 tile을 알아내기 위해 에러 기반의 tile mask를 만들자. naive한 방법은 pre-render된 이미지를 GT와 비교한 error map을 downsampling하는 것.
- 효율적인 error map 획득을 위해, 매 iter 대신 주기적으로 GT와 모델이 렌더링한 이미지를 비교하여 얻은 error map을 deprojection 시켜서 3D grid로 변환한 후, 필요할 때 이를 다시 projection하여 tile mask를 얻는 방법을 고려해보자.


결과물:
- tile 단위로 학습하는 4DGS 모델의 구현
- GT와 모델이 렌더링한 이미지의 error map을 활용하여 tile mask를 생성하는 방법
- tile 단위 학습이 전체 학습 속도에 미치는 영향에 대한 분석
- tile 단위 학습이 모델의 최종 품질에 미치는 영향에 대한 분석

정보:
- 학습 커맨드: `python train.py --config <config_file>`
- codex 작업 히스토리는 history 폴더 내에 저장됨

용어:
- tile ratio: 전체 이미지에서 학습이 필요한 tile의 비율. 낮을수록 학습 속도가 빨라질 것으로 예상됨.

규칙:
- 실험 환경은 conda 4dgs 환경에서 진행하기.
- sweep 실험등을 할 때 config 파일을 추가하기 보다는 커맨드라인 인자를 활용하는 스크립트 파일을 작성하기.
- 작업 과정을 히스토리에 기록하여 작업 내역이 추적될 수 있도록 하기.
- 실험 결과는 정량적 지표와 함께 시각적으로도 비교할 수 있도록 이미지나 그래프 형태로 저장하기.
- 시간 프로파일링은 warmup을 고려하여 300~500 iter 구간에서 측정하고, 평균으로 보고하기.
- 이미지 품질 평가를 위한 실험은 학습 시 batch size 1, 30000 iter에서 진행하고, 프로파일링은 끄기. 품질은 PSNR, SSIM, LPIPS(ALEXNET)으로 평가하고, 학습 시간을 포함하여 보고하기.
- 실험 결과는 동일한 조건에서 baseline 모델과 비교하여 보고하기.
- 답변은 가능한 간결하게, 최종 결과 보고나 문제가 생기는 경우에만 제공하기.